import { Routes } from '@angular/router';
import { PersonaListComponent } from './presentation/personas/components/persona-list/persona-list.component';

export const routes: Routes = [
	{ path: '', component: PersonaListComponent },
];
