import { Routes } from '@angular/router';
import { TablaAPIComponent } from './ui/views/tabla-api.component/tabla-api.component';

export const routes: Routes = [
  { path: '', component: TablaAPIComponent }
];